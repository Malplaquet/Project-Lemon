<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, user-scalable=no">
  <title>{% block title %}Welcome! o/{% endblock %}</title>
  <link rel="stylesheet" href="https://bootswatch.com/4/sandstone/bootstrap.min.css">
  {% block stylesheets %}{% endblock %}
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('index') }}">Lemon Project</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse">
      {% if not app.user %}
      <div class="form-inline my-2 my-lg-0">
        <a href="{{ path('security_login') }}"><button class="btn btn-primary my-2 my-sm-0" type="button">Connexion</button></a>
      </div>
      {% else %}
      <div class="form-inline my-2 my-lg-0">
        <a href="{{ path('gestion') }}"><button class="btn btn-primary my-2 my-sm-0" type="button">Administration</button></a>
      </div>
      <div class="form-inline my-2 my-lg-0">
        <a href="{{ path('security_logout') }}"><button class="btn btn-secondary my-2 my-sm-0" type="button" name="button">Deconnexion</button></a>
      </div>
      {% endif %}
    </div>
  </nav>
  <div class="container">
    {% block body %}{% endblock %}
  </div>
  {% block javascripts %}{% endblock %}
</body>
</html>
